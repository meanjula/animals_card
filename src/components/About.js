import React from "react";
const About = () => {
  return (
    <div className="about">
      <p>
        This is the Animals app created using react and react router. This is an
        example of single page application as different page renders without any
        page reload.
      </p>
    </div>
  );
};
export default About;
